import { BasePage } from './BasePage';\n\nexport class LoginPage extends BasePage {\n  async login(email: string, password: string) {\n    await this.fill('input[name=\"email\"]', email);\n    await this.fill('input[name=\"password\"]', password);\n    await this.click('button:has-text(\"Sign In\")');\n  }\n\n  async expectLoginPage() {\n    await this.expectVisible('text=Welcome to Genic Teams');\n  }\n\n  async expectLoginError() {\n    await this.expectVisible('text=Invalid credentials'); // Assuming error on failure\n  }\n}