import { test, expect } from '../fixtures/base.fixtures';\n\ntest.describe('Login Page Tests', () => {\n  test('should login successfully with valid credentials', async ({ loginPage }) => {\n    await loginPage.goto('/');\n    await loginPage.expectLoginPage();\n    await loginPage.login('admin@bthrust.com', 'password');\n    // Add checks for successful login\n  });\n\n  test('should show error with invalid credentials', async ({ loginPage }) => {\n    await loginPage.goto('/');\n    await loginPage.expectLoginPage();\n    await loginPage.login('invalid@bthrust.com', 'wrongpassword');\n    await loginPage.expectLoginError();\n  });\n\n  test('should navigate to forgot password', async ({ page, loginPage }) => {\n    await loginPage.goto('/');\n    await loginPage.expectLoginPage();\n    await page.click('text=Forgot Password ?');\n    // Check navigation to forgot password page if needed\n  });\n});